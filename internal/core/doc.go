// Package core defines Gravy's domain types and the ticket state machine.
//
// It performs no I/O and imports nothing outside the standard library. Every other
// package may depend on it; it depends on none of them.
package core
